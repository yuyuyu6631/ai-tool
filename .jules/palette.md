## 2024-06-27 - 标签移除按钮的无障碍改进
**Learning:** 当按钮的可见文本仅为项目名称（如带有“X”图标的工具标签）时，屏幕阅读器只会朗读该名称，无法向用户传达按钮隐含的“移除”操作意图。
**Action:** 为此类基于名称的移除按钮提供描述性的 `aria-label`（例如 `Remove ${tool.name}`）。当上下文或动态变量可能以英文为主时，应使用英文标签以防止英文屏幕阅读器出现语言解析回归。
