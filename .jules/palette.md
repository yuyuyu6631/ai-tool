## 2025-02-20 - [可折叠组件的 ARIA 属性最佳实践]
**Learning:** 在使用屏幕阅读器时，像 AI 思考过程这样的可折叠组件必须要加上 `aria-expanded` 属性来告知当前的折叠/展开状态，同时即便按钮已有可见文本，依然应该补充一个清晰明确的英语 `aria-label`，以兼容英文系统并保证无障碍功能的完备性。
**Action:** 在实现所有的展开/收起切换按钮时，必须附加上 `aria-expanded={isExpanded}` 以及相应的 `aria-label`，从而保证各屏幕阅读器用户的一致性和无障碍体验。
