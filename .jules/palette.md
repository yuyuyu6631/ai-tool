## 2024-07-07 - 完善动态工具列表项的无障碍化
**Learning:** 当在动态列表（如对比工具栏）中渲染带有操作图标和名称的按钮时，仅保留名称作为可访问名称会导致语义不清晰（读屏器只会朗读工具名称），且未隐藏的装饰性图标会导致读屏器重复播报或产生冗余信息。
**Action:** 总是为具有特定操作意图（如“取消对比”、“移除”）的组合式按钮添加准确描述操作的 `aria-label` 和 `title`，并确保嵌套的装饰性图标使用 `aria-hidden="true"` 隐藏。
