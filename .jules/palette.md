## 2025-06-30 - 动态工具移除按钮无障碍化
**Learning:** 当展示包含"X"图标的动态标签页（如对比工具列表）且不包含针对屏幕阅读器优化的文字时，用户可能无法辨别此按钮的功能，仅凭组件的文本名称难以明确该按钮是执行“移除”操作。
**Action:** 确保为可关闭的标签页或者移除按钮添加明确且能够描述该移除对象(如 `Remove ${tool.name}`)的 `aria-label` 以保证无障碍体验。在使用英文环境系统时，为了避免兼容问题，`aria-label`应保持英文。
