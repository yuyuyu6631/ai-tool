## 2024-06-04 - 为动态列表项添加上下文操作描述
**Learning:** 在包含文本和图标（如关闭图标）的按钮中，如果仅包含项目名称（例如 `tool.name`），屏幕阅读器用户将无法理解该按钮的具体操作（如“移除”）。仅仅依赖视觉上的 X 图标是不够的。
**Action:** 为包含“项目名称”且具有删除/移除功能的按钮，添加清晰的 `aria-label`，如 `aria-label={"移除 " + tool.name}`，以准确传达交互意图。
