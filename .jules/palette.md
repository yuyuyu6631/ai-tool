## 2024-06-20 - 改进仅包含项目名称和图标按钮的无障碍体验
**Learning:** 当按钮的可见文本仅为项目名称（例如，带有“X”图标的已选标签）时，屏幕阅读器仅会朗读项目名称，而无法传达按钮的实际操作意图（移除/删除）。
**Action:** 始终为这类按钮提供描述性的 `aria-label`（例如 `Remove ${itemName}`），以确保屏幕阅读器能准确传达其操作意图，同时在周围上下文主要是英语时使用英文 `aria-label` 避免多语言朗读冲突。
