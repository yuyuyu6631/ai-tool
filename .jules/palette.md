## 2024-06-05 - 动态标签移除按钮的无障碍化
**Learning:** 当按钮中同时包含可视文本（如项目名称）和仅用作视觉提示的图标（如 "X" 关闭图标）时，如果缺乏明确的 `aria-label`，屏幕阅读器将仅朗读项目名称，这无法准确向视障用户传达该按钮的实际交互目的（例如“移除”）。
**Action:** 在设计此类组合式标签按钮时，应始终添加显式的 `aria-label`（如 `Remove ${itemName}`），并在其中包含原本的可视文本，以同时满足 WCAG 的“Label in Name”规则并明确传达交互功能。
