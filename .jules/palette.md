## 2025-02-15 - 为仅包含图标的按钮的图标添加 aria-hidden
**Learning:** 当仅包含图标的按钮（如移除项、关闭菜单、主题切换）已经有了控制可访问性名称的 `aria-label` 时，屏幕阅读器可能会重复播报内部包含的 `<svg>` 或图标元素。
**Action:** 总是为这些通过 `aria-label` 获得可访问性名称的按钮的内部图形图标（例如 `<X />`, `<Menu />`, `<Sun />`）显式添加 `aria-hidden="true"`。
