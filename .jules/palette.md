## 2024-06-26 - 移动端菜单按钮无障碍化
**Learning:** 对于只包含图标的展开/收起切换按钮，仅仅根据状态动态更改 `aria-label`（如“打开导航”或“关闭导航”）会导致屏幕阅读器无法清晰传达这是一个控制其它内容区域状态的开关组件。
**Action:** 在实现折叠面板或侧边栏切换按钮时，应使用描述其本身功能的固定 `aria-label`，并始终配合 `aria-expanded` 和 `aria-controls` 属性，以提供最标准、语义最明确的辅助体验。
