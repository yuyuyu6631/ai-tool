
## 2024-06-19 - 移动端菜单切换按钮无障碍优化
**Learning:** 在实现纯图标的折叠/展开控件时，如果通过动态切换 `aria-label`（如“打开/关闭”）来表达状态，屏幕阅读器用户可能会感到困惑。这违反了将状态与功能标签分离的无障碍原则。
**Action:** 始终为图标切换按钮提供一个固定的描述性 `aria-label`（如“导航菜单”），并通过 `aria-expanded={boolean}` 和 `aria-controls="id"` 属性向屏幕阅读器准确传达当前的展开/折叠状态。
