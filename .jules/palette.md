## 2026-06-10 - [标签按钮增加 ARIA 标签]
**Learning:** 在带有移除功能的标签按钮中，如果可见文本只有条目名称和一个 'X' 图标，屏幕阅读器往往只会读出名称而丢失操作上下文。提供包含操作动词的描述性 `aria-label` (如 `Remove ${itemName}`) 对无障碍体验至关重要。
**Action:** 以后遇到带有删除/移除图标的标签组或卡片时，务必加上描述性的 `aria-label`。
