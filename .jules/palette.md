## 2025-02-23 - [聊天助手纯图标按钮的无障碍化]
**Learning:** 在聊天助手组件的标题栏中，纯图标按钮（如“新对话”和“关闭”）如果没有 `aria-label` 且内部的 SVG 元素没有 `aria-hidden="true"`，会导致屏幕阅读器用户无法知道这些按钮的实际用途。
**Action:** 在编写或修改纯图标按钮时，必须始终添加描述该操作的 `aria-label`，并将嵌套的装饰性 SVG 元素设置为 `aria-hidden="true"`，以防止屏幕阅读器重复朗读。
