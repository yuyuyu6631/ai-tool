## 2025-07-05 - 避免屏幕阅读器重复播报图标
**Learning:** 当按钮通过 `aria-label` 提供了完整的无障碍名称时，其内部嵌套的 SVG 图标（如 `<X />`, `<Send />`）必须显式添加 `aria-hidden="true"`，否则部分屏幕阅读器可能会尝试播报无意义的图标元素，导致冗余和困惑。
**Action:** 以后为纯图标按钮添加 `aria-label` 时，始终确保内部的 `<svg>` 或图标组件带有 `aria-hidden="true"` 属性。
