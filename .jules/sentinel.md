## 2025-02-24 - 添加缺失的管理员端点认证
**Vulnerability:** 管理员专用的API端点（`/crawl/jobs` 和 `/extract`）在其路由器配置中缺少必要的认证和授权检查。
**Learning:** 即使功能主要是供内部或后台使用，任何操作数据库状态或使用大量资源（如爬虫或LLM API）的端点也必须显式地声明并强制执行鉴权依赖。
**Prevention:** 在创建新的管理或内部API路由组时，应始终在 `APIRouter` 实例化时添加认证依赖，以确保组内所有端点默认受到保护，而不是在单个函数上遗漏。
