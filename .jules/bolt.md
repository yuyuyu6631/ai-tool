## 2025-03-08 - 优化 Fuse.js 的搜索性能
**经验：** 在进行客户端模糊搜索时，不要对所有结果进行 map 和 slice 操作，而是直接向 `fuse.search` 传递 `limit` 选项。这可以显著提升搜索速度，尤其是在数据集较大时。
**行动：** 在后续实现任何客户端搜索或过滤逻辑时，始终检查底层库是否支持内置的分页或限制结果数量的参数，以避免全量计算和渲染前的大范围截断。
