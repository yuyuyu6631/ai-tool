## 2024-10-24 - 优化 Fuse.js 客户端搜索性能
**经验：** 在处理客户端模糊搜索时，Fuse.js 默认会返回并处理所有匹配项。如果在调用 `search` 后再使用 `slice` 截断，会导致不必要的大量对象分配和映射。
**行动：** 对于只需要前 N 个结果的场景，始终通过将 `limit` 参数直接传递给 `fuse.search(query, { limit: 10 })` 来限制底层库搜索输出，提升大型数据集搜索效率。
