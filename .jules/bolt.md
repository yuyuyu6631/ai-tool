## 2024-07-03 - ⚡ Bolt：优化客户端 Fuse.js 搜索性能
**经验：** 在处理客户端的 Fuse.js 搜索结果时，应该直接在搜索选项中提供 `limit` 参数。如果先执行全量搜索返回结果，然后再通过 `.map()` 映射并利用 `.slice()` 切片，不仅会产生中间多余的对象，在庞大结果集下还会大幅增加主线程执行时间和内存消耗。
**行动：** 在使用如 Fuse.js 的模糊搜索库时，始终检查并直接利用内置的配置项（如 `limit`）在引擎层面截断结果，而非在取得全量数据后再进行后处理。
