## 2024-07-08 - 优化全局搜索命令面板
**经验：** 在 React 组件中处理高频用户输入（如搜索框）并触发复杂计算（如 Fuse.js 的模糊搜索和 NLU 意图解析）时，如果直接使用受控组件的值进行计算，容易导致主线程阻塞和输入卡顿。
**行动：** 在类似的场景中（尤其是涉及本地计算和大量数据渲染），优先使用 React 18+ 的 `useDeferredValue` 钩子来推迟计算，或者使用传统的 debounce/throttle。`useDeferredValue` 更符合 React 的并发渲染模型，能够在不阻塞 UI 交互的前提下平滑地更新依赖项。
