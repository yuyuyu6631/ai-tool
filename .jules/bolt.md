## 2025-02-27 - Fuse.js 客户端搜索的性能瓶颈
**经验：** 在处理大型数据集时，使用 Fuse.js 进行客户端搜索并在外部通过 `slice` 截断结果会引起显著的性能衰退，因为这会导致对整个结果集进行不必要的评分和排序操作。
**行动：** 始终将 `{ limit: N }` 作为参数直接传递给 `fuse.search()` 方法，以便在搜索层级进行截断，大幅优化大数据集下的搜索性能。
