## 2024-06-26 - Fuse.js 客户端搜索性能优化
**经验：** 在大型数据集中使用 Fuse.js 进行客户端搜索时，如果不限制搜索结果，Fuse 会遍历整个数据集并对所有匹配项进行打分和排序。通过向 `fuse.search` 传递 `limit` 选项，可以极大地提升性能，避免先 map 然后再 slice 的无用开销。
**行动：** 下次在任何地方使用 Fuse.js 时，优先查看是否可以通过 `limit` 限制结果返回数量。
