## 2024-05-24 - 优化 Fuse.js 客户端搜索性能
**经验：** 在处理客户端大数据集搜索时，Fuse.js 的默认搜索会返回所有匹配项，随后再执行 map 和 slice 操作会导致不必要的计算性能开销。
**行动：** 使用 Fuse.js 进行搜索时，应当直接向 search 方法传递 limit 配置选项（例如 fuse.search(query, { limit: 10 })），从而在源头上限制返回的结果集大小，提升搜索效率。
