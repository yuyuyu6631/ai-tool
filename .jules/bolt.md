## 2026-05-31 - 优化类别别名解析以避免 O(N) 退化
**经验：** 在处理目录的过滤和渲染时，如果在循环内部使用生成器表达式去遍历字典的 `items()` 以解析别名，会导致针对每个元素都产生 O(N) 的查找开销。这种模式在大量数据下会引发严重的性能瓶颈。
**行动：** 对于类似于 `LEGACY_CATEGORY_SLUGS` 的别名映射，在模块级别使用一次遍历生成反向查找字典（`_REVERSE_CATEGORY_SLUGS`），后续的查找一律使用该字典的 O(1) `get` 方法来替代运行时的线性扫描。
