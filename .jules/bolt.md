## 2024-03-24 - Fuse.js 客户端搜索性能瓶颈
**经验：** 在处理 Fuse.js 客户端搜索时，如果不直接传入 `limit` 参数，在处理大型数据集时默认会计算并返回匹配的全部结果，随后再通过前端进行切片截断，会导致大量不必要的对象分配和数组操作，严重影响主线程性能。
**行动：** 对于 Fuse.js 搜索调用，始终直接在 `search` 方法选项中指定 `{ limit: N }` 选项，而非在前端执行 `.slice()` 截断。
