## 2026-05-07 - 优化 Fuse.js 客户端搜索性能
**经验：** 在处理 Fuse.js 搜索时，先搜索全体数据再执行 `.slice(0, limit)` 在面对大数据集时会有明显性能损耗，因为底层要计算并排序所有匹配项。
**行动：** 对于只需要前 N 个结果的搜索场景，始终将 `{ limit: N }` 选项直接传递给 `fuse.search` 方法，大幅减少内部排序开销。
