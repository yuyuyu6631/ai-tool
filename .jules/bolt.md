## 2026-06-07 - 目录分类别名解析引发的 O(N) 性能降级
**经验：** 在进行目录操作（如 _filter_tools，get_home_catalog 等）时，如果在循环中使用生成器表达式遍历字典 LEGACY_CATEGORY_SLUGS.items() 来解析别名，会导致重复执行昂贵的循环操作而引发 O(N) 的性能损耗。这在拥有大量工具和分类时会显著降低后端接口性能。
**行动：** 应当使用预先计算好的反向映射字典（如 _REVERSE_CATEGORY_SLUGS）来实现 O(1) 的别名查找，避免在循环中重复执行昂贵的查询。
