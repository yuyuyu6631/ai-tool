## 2024-05-26 - 预计算反向映射字典优化分类解析
**经验：** 在处理大量工具目录（如 `get_home_catalog` 或大列表过滤 `_filter_tools`）时，每次判断分类映射关系时迭代执行 O(N) 复杂度的生成器表达式（在 `LEGACY_CATEGORY_SLUGS` 较长或调用较频繁时）会导致明显的性能衰减。
**行动：** 对于固定不变的分类映射或类似字典结构，使用模块级别的预计算字典（O(1) 查找）代替动态生成的查询，特别是在被高频调用的过滤条件中。
