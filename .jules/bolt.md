## 2025-02-28 - Fuse.js 搜索性能优化
**经验：** 在处理客户端搜索时，Fuse.js 提供了内建的 `limit` 选项。如果先进行全量搜索然后再通过 `slice` 截取结果（如 `fuse.search(query).map(...).slice(0, 10)`），在大型数据集上会显著降低性能。使用内建选项可以将性能提升，避免不必要的处理。
**行动：** 对于所有的客户端 Fuse.js 搜索，始终直接在搜索配置中传递 `{ limit: N }` 选项，而不是使用数组操作 `slice(0, N)`。
