## 2024-06-18 - [Fuse.js 客户端搜索性能优化]
**经验：** 在处理客户端大数据集搜索时，如果在映射搜索结果之前不限制数量，会导致处理不必要的内存分配和循环，特别是映射与切片带来的 O(n) 开销。将 limit 参数直接传给 fuse.search 可以显著减少内存开销和 JS 主线程耗时。
**行动：** 下次使用 Fuse.js 等搜索引擎时，始终将分页/限制逻辑下推给搜索实例本身，不要在外部通过 map/slice 进行数据截断。
