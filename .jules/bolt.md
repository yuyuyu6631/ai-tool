## 2024-05-24 - [Fuse.js 客户端搜索性能优化]
**经验：** Fuse.js 默认会搜索并计算所有匹配结果。当本地缓存的工具数据集较大时，先获取全部结果再通过 map 映射和 slice 截断，会导致不必要的内存分配和性能下降。
**行动：** 对于 Fuse.js 的客户端搜索操作，始终应将 limit 选项直接传入 search 方法 (如 `fuse.search(query, { limit: 10 })`)，使其内部尽早截断结果，从而显著提升搜索性能。
