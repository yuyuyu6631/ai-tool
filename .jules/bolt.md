## 2024-05-24 - [Fuse.js 客户端搜索性能优化]
**经验：** 在处理大规模数据集时，对 Fuse.js 的搜索结果先 map 再 slice 会造成大量不必要的评分计算和内存分配。Fuse.js 原生支持 limit 参数，可以在搜索底层阶段提前熔断，避免全量排序和后续数组截断带来的性能开销。
**行动：** 下次在客户端实现基于 Fuse.js 的搜索功能时，务必将提取数量限制（如 `limit: 10`）直接通过选项传入 `fuse.search(query, { limit: n })`，而非在返回完整结果后再通过 JS 原生数组操作截断。
