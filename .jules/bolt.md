## 2025-07-07 - 搜索面板的性能瓶颈
**经验：** 在全局搜索组件（CommandPalette）中，发现输入框直接绑定了 NLU 解析和 Fuse.js 模糊匹配。这些计算操作相对昂贵，在用户快速打字时会阻塞主线程，导致明显的输入卡顿，这是一种典型的 React 渲染性能反模式。
**行动：** 对于此类依赖大量计算的输入反馈，应统一引入 `useDeferredValue`（或防抖），将计算与立即呈现的 UI 状态分离，确保用户的输入体验始终如闪电般顺畅。
