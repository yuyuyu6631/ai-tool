## 2026-06-12 - [Fuse.js 客户端搜索性能优化]
**经验：** 在处理大型数据集时，使用 Fuse.js 进行客户端搜索，如果不传递 limit 参数，会导致对所有匹配结果进行排序和全量数组 map 操作，这在大数据集下会引起显著的性能开销。
**行动：** 使用 `fuse.search(query, { limit: 10 })` 将限制条件直接传递给搜索方法，让引擎在内部截断，然后再进行映射，可以有效减少处理时间并降低内存占用。
