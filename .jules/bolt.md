## 2025-03-08 - 优化 Fuse.js 搜索性能
**经验：** 在处理 Fuse.js 客户端搜索时，应该将 `limit` 选项直接传递给 `fuse.search(query, { limit: n })`，而不是在获取所有结果后使用 `slice` 截取。这样可以显著提升大数据集下的搜索性能。
**行动：** 下次在客户端实现搜索功能时，优先使用库原生支持的 limit 参数限制返回集大小，避免不必要的遍历和内存占用。
