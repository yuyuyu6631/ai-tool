## 2026-06-08 - 优化 Fuse.js 搜索性能
**经验：** 在处理大规模数据集的客户端搜索时，对所有的 fuse.search 结果执行 map 并之后做 slice(0, 10) 是极度低效的，因为 Fuse.js 需要计算所有匹配项。将 limit 参数直接传给 fuse.search(query, { limit: 10 }) 可以在底层提早终止计算并极大提升性能。
**行动：** 下次使用 Fuse.js 等搜索引擎时，始终将结果数量限制参数传入其原生配置，而非在外部使用数组切片。
