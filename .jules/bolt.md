## 2025-02-15 - Fuse.js 搜索限制优化
**经验：** 在处理客户端模糊搜索时，对所有结果进行 `.map` 然后 `.slice` 处理会带来不必要的性能损耗，特别是当数据集庞大时。Fuse.js 原生支持 `{ limit: N }` 选项。
**行动：** 下次使用 Fuse.js 进行搜索时，优先在查询配置中传递 `limit` 参数，而不是在返回结果后手动截取，以提高性能和减少内存占用。
