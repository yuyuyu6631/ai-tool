## 2024-05-18 - [优化 Fuse.js 客户端搜索性能]
**经验：** 在大型数据集上执行 Fuse.js 搜索时，如果不限制返回结果的数量而是在返回整个数组后再使用 `slice` 进行截断，会造成严重的性能浪费。将 `limit` 选项直接传给 `fuse.search` 方法可以显著提升检索效率。
**行动：** 在处理客户端搜索时，始终将分页或截断限制（如 `{ limit: X }`）作为选项传入搜索方法，避免在内存中对无用数据进行遍历和操作。
