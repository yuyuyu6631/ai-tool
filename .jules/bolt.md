## 2026-05-17 - [消除类别别名解析中的 O(N) 性能瓶颈]
**经验：** 在处理商品或类别目录的频繁过滤查询时，迭代查找列表或生成器解析 (比如使用 `next(item for ...)`) 会成为 `O(N)` 的瓶颈。特别是类别存在别名时，针对每个工具不断重复扫描包含所有类别的集合将带来明显的性能损耗。
**行动：** 在服务模块初始化时预先构建一个 `O(1)` 的反向映射字典（如 `_REVERSE_CATEGORY_SLUGS`），并在处理数据或加载缓存时统一使用该字典替换原有的生成器表达式和循环扫描查找，从而消减了 O(N) 重复迭代。
