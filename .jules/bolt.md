## 2026-06-03 - 消除批量更新中的 N+1 查询
**经验：** 在执行类似 `upsert_match_plan` 的批量更新操作时，基于循环单独执行的 `db.delete()` 和 `db.scalar()` 会显著导致 N+1 查询性能瓶颈，随数据量增加大幅减缓数据库操作。
**行动：** 在处理批量增删改时，始终利用 `db.execute(delete(Model).where(...))` 替代逐个删除，并使用 `.in_()` 批量查询记录后再在内存中匹配，将 O(N) 数据库操作降低为常数次请求。
