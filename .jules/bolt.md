## 2026-06-01 - [O(1) 反向映射优化工具类目查询]
**经验：** 在处理类目别名解析时（如 `LEGACY_CATEGORY_SLUGS`），在循环中每次调用 `.items()` 会导致 O(N) 的性能退化。这在列表过滤和首页数据聚合中被大量使用，形成隐性的性能瓶颈。
**行动：** 在应用启动时或模块加载时预先构建基于哈希表的反向映射字典 (`_REVERSE_CATEGORY_SLUGS`)，将查询时间复杂度由 O(N) 降低至 O(1)。
