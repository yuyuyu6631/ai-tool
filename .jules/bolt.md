## 2024-05-18 - 优化客户端搜索性能
**经验：** 在处理大型数据集时，使用 Fuse.js 进行客户端搜索，如果不传递 `limit` 参数，它会对所有数据进行匹配、打分和排序。这在数据集较大时会造成主线程阻塞。
**行动：** 对于 Fuse.js 的 `search` 方法，应始终通过选项对象直接传递 `{ limit: N }`，而不是在获取全量结果后再执行 `.slice(0, N)`。
