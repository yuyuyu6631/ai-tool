## 2026-06-05 - [缓存别名反向映射优化]
**经验：** 在类别等数据处理循环内部使用字典的 `.items()` 和生成器表达式进行反向查找时，会导致 O(N) 的性能退化。这在批量数据过滤（如分类目录展示）时尤其明显。
**行动：** 在模块加载阶段预计算 O(1) 的反向映射字典（如 `_REVERSE_CATEGORY_SLUGS`），将循环中的嵌套查找转化为直接的哈希表查找。
